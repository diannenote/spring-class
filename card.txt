 CREATE TABLE TICKET 
   (	CONSUMERID VARCHAR2(5 BYTE), 
    	COUNTNUM NUMBER(5,0)
   );
 ALTER TABLE TICKET ADD CONSTRAINT CHECKNUM CHECK (CountNum < 5) ENABLE;
 CREATE TABLE CARD 
       (	CONSUMERID VARCHAR2(5 BYTE), 
        	AMOUNT NUMBER(5,0)
        ) ;